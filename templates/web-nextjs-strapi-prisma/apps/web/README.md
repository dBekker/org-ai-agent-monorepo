# apps/web

Frontend application shell.

## Rules
- keep route assembly here
- keep shared primitives in `packages/ui`
- keep Strapi mapping code in `lib/strapi/`
- default to Server Components
- use client components only where needed
